# 302 首个可玩原型验收

日期：2026-09-11。范围：第一章单个维修工单的完整流程。

## 实现状态

Blender 负责原创公寓与工具资产，Godot 负责第一人称交互、维修状态、实时画面与声音。厨房、客厅、公共走廊可步行连通；场景资源与可编辑 `.blend` 已交付。

本轮完成观察与测量、旁路接管泄载、解锁调向、锁定复测、封盖签收，以及返回打印机领取 401 工单的结尾。错误操作可逆，不会因异常转移而要求重开。加入自动存档、设置、按键重映射和按 H 获取当前步骤提示。

## 自动验证

通过 `bash tools/test.sh`：

- 维修状态机：317 项断言，0 失败。包括正常顺序、错误顺序、验收条件、恢复存档，以及 150 组随机操作后的可恢复性。
- 实际场景集成：53 项断言，0 失败。使用生产场景的物理射线与输入路径，覆盖 11 个交互目标、扳手长按、右键读数、异常转移与泄载、签收和结尾。
- 连通性：生产角色控制器从走廊进入厨房、穿过门洞进入客厅，再返回走廊；另验证墙壁阻挡角色和隔墙交互。
- 存档：独立测试文件写入、读取恢复、无效外层数据回退；设置值与重映射同时反映到角色与界面。

测试使用独立的 `integration302-*` 用户数据文件，结束后清理。正常游玩的 `apartment302-*` 文件不被自动测试覆盖。

## 实机与界面检查

在当前 Mac / Apple M5 上以 Godot 4.7.2、Metal Forward+ 运行。1280 × 800 窗口的短时固定视角截图读到约 120 FPS；这不是长期性能基准，也不代表最低帧率。

已通过原生窗口操作检查：领取工单、设置面板、Tab 打开和关闭工单、Esc 暂停、菜单退出。游戏截图使用实际 Godot 视口；厨房和检修箱均已逐图检查。

启动入口 `开始游戏.command` 已执行，能够导入资源并启动项目。Shell 脚本已通过 `bash -n` 语法检查。

## 视觉评审与后续范围

已落实低饱和冷光、局部检修暖光、旧漆柜体、墙面污迹、CC0 PBR 灰泥与水磨石、雨窗和窗外楼体。主菜单与工单使用克制的纸张和维护记录语言。

目前仍是功能与氛围原型，和已确认概念图存在明确差距：住宅细节密度较低，手部与道具动画较简单，金属磨损和局部灯光需要进一步精修。声音是项目内生成的临时声音，未经过正式音效设计与主观听音验收。后续应先完成一间厨房的美术精修，再扩展关卡。

401 仅为结尾提示，第二关、完整剧情、配音、商店与独立签名发行包均未包含。本轮没有发布或上传游戏。
